# UI screenshots

Place dashboard screenshots here for the root `README.md` (relative paths from repo root).

Suggested filenames (referenced in README):

- `dashboard-overview.png` — main dashboard with tab navigation
- `compare-tab.png` — Compare tab (intensity table and chart)
- `pooling-tab.png` — Pooling tab with member list

After capturing, commit the PNG files so they render on GitHub.
